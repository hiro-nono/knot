ORCAROUTER_BASE_URL=
ORCAROUTER_API_KEY=

AI_MODEL=

# Server
PORT=
GIN_MODE=

# Database (Postgres)
POSTGRES_HOST=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=

# Supabase (ID Provider)
# プロジェクトのベースURL(例: https://xxxx.supabase.co)。JWT検証用の公開鍵セット
# (JWKS)を取得するために使う。app/.env.example の NEXT_PUBLIC_SUPABASE_URL と同じ値。
# 秘密情報ではない。
SUPABASE_URL=

# CORS (comma-separated list of allowed origins)
CORS_ALLOWED_ORIGINS=